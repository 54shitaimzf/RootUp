# 0.8.6 数据库审计报告

> 日期：2026-08-10；命令：`scripts/check-db.ps1`（真实 AppData 索引库）。

## 结果

| 检查项 | 结果 |
| --- | --- |
| integrity_check | PASS |
| foreign_key_check | PASS（0 违规） |
| user_version | PASS（=7，0.8.6 期望） |
| journal_mode | PASS（WAL） |
| 重复 path_key | PASS（0） |
| 异常标签 | PASS（0） |
| 无源归档记录 | PASS（0） |
| 查询索引（state/modified） | PASS |
| FTS5 延迟建表 | PASS（决策门未启用，表不存在为预期） |
| usn_state 表 | PASS（存在） |
| 标签/AND 查询计划 | PASS（EXPLAIN 捕获） |
| settings.json | 缺失 → 应用回退默认（见下） |
| study / labels / schemes / habits JSON | PASS（可解析，结构完整） |
| habits lastUsed/last_used | PASS |

合计：19 项检查，0 失败。

## 数据环境清理说明

- 审计前 AppData 中的 `rootup.db` 为 mft/UI 审查测试残留（50k 语料），`settings.json` 已被旧的测试链删除且无备份。
- 已将该目录全部文件归档至 `%LOCALAPPDATA%\com.rootup.desktop-archive\pre-release-20260810`（可恢复），并移除测试残留索引库；应用将以默认设置 + 空索引启动。
- 工具链修复：`benchmark.ps1` / `smoke.ps1` 还原逻辑改为「无备份不删除、失败即报错」；`bench-cleanup.ps1` 仅在活文件存在时清理备份，缺失时保留并告警，防止再次误删。
