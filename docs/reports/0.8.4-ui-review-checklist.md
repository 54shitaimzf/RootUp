# 0.8.4 UI 审查清单

> 审查对象：`src-tauri/target/release/rootup.exe`（0.8.4，`--no-bundle` 构建）。
> 数据环境：`scripts/seed-ui-review.ps1` 写入的边界数据；审查已完成，真实数据已通过 `-Restore` 还原。

## 重点审查区域

- [x] 文件列表排序：名称/类型/大小/修改时间/标签五维升降序切换正确，中文与数字混合排序稳定（多标签查询修复后实测 73/81 条结果正常）
- [x] 加载更多与滚动位置：翻页后列表追加不跳动、页码与总数显示正确（日志确认 offset=50 加载）
- [x] 文件列表边界数据：超长文件名截断、Unicode/隐藏/无扩展名/同名不同大小写/大文件/未来与远古修改时间（90 个边界文件全部入库）
- [x] 设置校验：目录非法值（保留名/非法字符/盘根）即时提示；归档设置保存防抖后落盘（后端强校验 + 前端镜像，边界矩阵测试全绿）
- [x] DirectoryAdder 错误态：粘贴非法路径、拖拽文件取父目录、常用目录一键添加（校验矩阵全绿）
- [x] 学业页边界数据：1 周/30 周/跨年学期、单双周与指定周范围课程、长标题课程、临期/逾期/完成/归档作业（4 学期/18 课程/11 作业全部加载）
- [x] 标签与方案：未知图标/颜色回退、标签列表上限附近、方案应用/保存/重命名/删除（97 标签/19 方案种子验证通过）
- [x] 深色/浅色与中英切换：上述界面在四象限下均可读（深浅/中英一致性纳入发布验收）

## 审查结论

- 共收到 5 项反馈并全部闭环：搜索框标签时按钮偏移（P2）、多标签/组合查询参数错误（P1）、习惯保存失败（P1）、单文件列表底部区域过高（P3）、滚动条占位宽度跳变（P3）。
- 修复后门禁：Rust 298 项、前端 402 项、`pre-release-check.ps1` 12/12 PASS；最终 release 已重建。
- 审查数据环境已还原，真实用户数据恢复并校验（settings/study/labels/schemes/rootup.db）。

## 反馈分级

- P1：功能错误或数据丢失 → 必须修复后重跑门禁
- P2：明显体验缺陷 → 必须修复后重跑门禁
- P3/P4：风格/建议类 → 记录为 0.8.5 候选

## 还原

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\seed-ui-review.ps1 -Restore
```
