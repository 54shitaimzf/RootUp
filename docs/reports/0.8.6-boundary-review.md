# 0.8.6 边界与架构审查（发布审批前）

> 日期：2026-08-09；范围：0.8.6 阶段一已提交内容 + 阶段二已实现部分（虚拟滚动 / release profile / 页面级分包 / MFT 验证脚本）。
> 结论：自动化与人工核对无未决违规；剩余发布门禁（官方基线、冒烟、Agent、pre-release）与 MFT 提权实验按计划在发布门执行。

## 1. 自动化复核

| 门禁 | 结果 |
| --- | --- |
| 前端全量 | PASS：51 文件 / 450 用例（含虚拟滚动纯函数与集成用例） |
| Rust 全量 | PASS：322 用例；fmt / clippy -D warnings 全绿 |
| 前端分层 | PASS：check:arch（pages/features/components/lib 单向依赖） |
| Rust 分层 | PASS：check:arch:rust（core 纯业务、commands 不回指组合根） |
| 版本 / 表格 | PASS：check:version（0.8.5 阶段）、Markdown 表格 lint |
| 分包 | PASS：主包 gzip 94.8KB、FilePage 10.2KB；首包远低于 200KB 目标 |

## 2. 职责边界清单

- **增量数据源**：`core/delta.rs` 定义契约（DeltaKind / DeltaRecord / DeltaSource），`infra/usn_delta.rs` 实现 USN 启动补账；扫描器与 watcher 不感知实现细节，未来 0.8.8 配对迁移可直接替换映射。
- **MFT 枚举**：`infra/mft.rs` 实现 `FileEnumerator` 语义，门控（`ROOTUP_MFT_SCAN=1` + 管理员 + NTFS）+ 失败回退 walkdir；扫描器仅按结果选择，选择策略（阈值）预留为独立可配置项（`mft-verify.ps1` 输出阈值后落位）。
- **存储**：schema v5（usn_state）/ v6（FTS 版本占位）/ v7（移除 idx_files_type）；FTS 实现与同步保留但默认不建表，表缺失时写路径自动跳过——缓用隔离完整。
- **目录对账**：`core/path.rs::is_missing_dir_error` 纯判定 + scanner 对账 + `watched_dir_health` 命令 + 设置页标记；与 0.8.8 统一错误码体系预留衔接。
- **前端**：`lib/virtual.ts` 纯计算、`components/VirtualRows.tsx` 通用渲染（不依赖文件页业务）、FilePage 仅传入行渲染闭包；页面级 lazy 后 pages 仍是单向依赖，无循环引用。

## 3. 解耦与扩展性

- 新增一篇文章/规则/枚举实现均不改动 core 契约；基准指标命名进入统一注册表（with/without_type、dev 锚点）。
- MFT 选择策略以“一致性 + 规模阈值”双条件，阈值由实验产出后可配置，不写死逻辑。
- FTS 转正路径唯一（批量重建后启用建表 + 同步），实现与测试已保留。
- 0.8.6-dev 锚点（25H2 + UBR 8875）为 0.8.6 官方基线提供同 OS 对比基底；0.8.5 仅作绝对值参考（系统版本不同不可比）。

## 4. 0.8.7 衔接清单

- MFT 默认启用：等待 `mft-verify.ps1` 管理员结果（一致性 + 交叉点阈值）后把“MFT 优先、失败回退”设为默认。
- FTS 批量重建优化后转正（当前默认不建表）。
- 单元同构：file/project/software 统一 key 空间 + 图标统一消费路径（theme/icons + 数据图标注册表）。
- keyset/COUNT 后续治理与虚拟列表性能数据沉淀。
- 增量源配对迁移（DeltaSource）与 0.8.8 错误响应对账机制对接。

## 5. 发布前仍需执行的门禁（按计划 M5/M7）

- 版本五处切 0.8.6 + CHANGELOG `[0.8.6]` + README/ROADMAP/ARCHITECTURE 定稿 + 帮助中心 0.8.6 更新亮点。
- `bench-all -Full -Version 0.8.6 -Rounds 7` 官方基线（与 0.8.6-dev 同 OS 对比）+ bench-cleanup。
- smoke（扩展断言）、db-audit（v7 预期）、agent-acceptance ×2、pre-release-check 全量。
- MFT 管理员实验（`scripts/mft-verify.ps1`）→ 结果入 `benchmarks/mft-crossover.md` 并决定默认策略。
- 最终汇报（MFT 收益为主）→ 用户批准 → push main + tag v0.8.6 + Actions 验证。
