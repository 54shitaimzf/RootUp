# 索引维护复核（0.8.6 阶段一决策记录）

> 日期：2026-08-09；方法：引擎基准新增“有 / 无 `idx_files_type`”变体（建库 / 类型查询 / 重分类），同轮内对比。
> 注意：本机持续负载导致绝对数值膨胀（约 5 倍），结论只取**同轮相对对比**；0.8.6 官方安静环境基线在阶段二完成后统一复核。

## 数据（两轮 p50，ms）

| 指标 | 有 type 索引 | 无 type 索引 | 相对 |
| --- | --- | --- | --- |
| 建库 100k（轮 1 / 轮 2） | 814 / 2024 | 745 / 2006 | 无索引更快（-8% / -1%） |
| 类型查询（轮 1 / 轮 2） | 13.0 / 12.5 | 5.9 / 11.0 | 无索引更快或持平 |
| 重分类 100k | 271 | 待复核（指标名已修正） | — |

## 决策

- **移除 `idx_files_type`**（schema v7 幂等 DROP，`user_version` 3→7 全链路测试通过）。类型查询的提速主要由 `idx_files_modified` 的“排序 + LIMIT 提前终止”提供，type 索引无额外收益且增加写放大。
- 若 0.8.6 官方安静环境复核显示类型查询回退 ≥15%，再评估 `(state, file_type)` 部分索引。
