# Changelog

本项目的版本变更记录。首个公开发布为 v0.6.0；此前版本为开发期里程碑，按迭代整理。

## [Unreleased]

### 0.8.6 阶段一（监控与查询性能，开发中）

- 扫描优化（实验先行）：MFT 按 `$MFT` 映射对分 run 流式读取、USA 原地修复、紧凑索引与子树定向解析（小目录不再全卷重建路径）；与 walkdir 对齐 skip_roots（项目根/归档根整棵不索引）；USN 卷句柄改 `FILE_READ_DATA` 并携带真实日志 ID，本机启动补账可用；`batch_size` 默认 2000 + 多行 upsert；扫描分阶段计时（read/parse/resolve/db ms）。
- 原生枚举器实验与边界审查：`FindFirstFileW` 直取元数据（真实目录约 5.8x），等价全绿（Unicode / junction / 超长路径），补 `\\?\` 长路径与单条目错误续枚举；bench 门控不进默认构建，转正留 0.8.7，见 `benchmarks/enumerator-*.md`。
- 交叉点实验：1k/10k 时 walkdir 胜出（MFT 读全卷 2.6GB 固定成本），50k 起 MFT 胜出（5977ms vs 8828ms）；20k/30k 边界受语料冷缓存影响有噪声，阈值待 25k 确认（不影响默认策略）；真实目录 71,923 文件 MFT 与 walkdir 数量、大小、时间全等（attribute-list 大小兜底后 size diff=0）；读取微优化（`FILE_FLAG_SEQUENTIAL_SCAN` + 32MiB 块 + FTS 表存在性单次检查 + DB 子批 1000 + 枚举器快照）带来 MFT 总耗时约 5%–8% 可测量下降；默认扫描策略仍 walkdir。
- MFT 快速基线（原始 `$MFT`，布局参考 Linux-ntfs / ntfs-3g / Sleuth Kit，不复制代码）：FILE 记录解析、USA 修复、extent/元文件/重解析点跳过、硬链接主名策略、路径重建；`ROOTUP_MFT_SCAN=1` + 管理员门控，任何失败回退 walkdir。
- USN 启动补账：`DeltaSource` 契约 + `usn_state`（schema v5），应用关闭期间变更启动时一次性对齐，运行期 notify 为主不轮询；无基线自动记录当前 USN。
- FTS5 文本索引（schema v6 占位，未默认启用）：contentless + trigram 实现与等价测试保留；批量同步成本高，待“扫描后批量重建”优化后再启用，见 `benchmarks/fts-evaluation.md`。
- 索引维护复核：移除 `idx_files_type`（schema v7）——同轮有/无对比显示其无查询收益且增加写放大，见 `benchmarks/index-set-evaluation.md`。
- 监控目录缺失对账：系统报“路径不存在”时索引标记 deleted（重扫可恢复）+ 设置页缺失标记（`watched_dir_health`）。
- 冒烟与审计扩展：USN 降级日志断言、`files_fts` / `usn_state` 审计项。

## [0.8.5] - 2026-08-08

### 性能与查询（0.8.5）

- 查询加速：`files` 表按实测收窄为三个索引（state / modified / type），文本、标签、类型查询 p50 提升 70%+，OFFSET 翻页提升约 89%；keyset 游标分页（`nextCursor`）单页约 0.1ms，替代 OFFSET 深翻页。
- COUNT 治理：默认查询不再 COUNT（`total=-1`），仅首页无筛选返回精确总数；前端计数栏降级为“已显示 N 个”。
- 显式 AND 语法：`+label:b` 与 `label:a AND label:b` 双写（`+` 为主），同维度默认 OR 语义不变；搜索补全、帮助中心与边界矩阵同步。
- 标签过滤评估：逗号串 + LIKE 优于 json_each（实测约快一倍），保持现状不迁表；FTS5 trigram 文本查询约为 LIKE 的 2 倍速，决策记录为 0.8.6 候选。
- NTFS 快速扫描（决策门）：引入官方 `windows` crate，实现卷能力探测、USN 记录解析与路径重建；USN 不含文件大小且日志可能被裁剪，快速全量路径以 `ROOTUP_FAST_SCAN=1` 实验性门控，默认回退 walkdir 保证正确性；MFT 基线顺延 0.8.6，详见 `benchmarks/mft-usn-evaluation.md`。
- 分类加速：课程分类改用 Aho-Corasick 一次多模式匹配，含重叠命中等价测试；忽略规则因语义（前缀/精确/包裹）不适用 AC，保持线性。
- 性能基准：0.8.5 vs 0.8.4 单轨对比（58 项指标、引擎 Full + 系统 10k 7 轮）；查询大提速、扫描/写入为索引维护代价（10k 持平、100k 约 +40% 待安静环境复核），完整警示与 0.8.6 去向见 `docs/reports/0.8.5-release-report.md`。
- 帮助入口视觉去重：页头“查看本页帮助”改为“Info 图标 + 帮助文字”按钮，搜索框保留“?”语法帮助；两个入口语义不同（本页指南 vs 搜索语法）且视觉明确区分。
- 侧栏帮助中心入口与页头帮助按钮区分：左下角“帮助与新手入门”改为浅色圆角卡片（书本图标 + 短副标题），悬停悬浮提示补全完整内容，避免窄栏截断；版本号改为居中小徽标（薄荷小芽 + 等宽字体）。
- 发布卫生：冒烟测试结束后自动把临时目录的索引记录标记 deleted，不再向真实索引库遗留幽灵文件（曾导致文件页总数多出测试残留）。

### 易用性迭代（0.8.5 前置）

- 帮助中心升级为五分区：新增“任务指南”（7 篇流程式文章）与“遇到问题”（7 篇故障排查）分区，文章可展开步骤、跳转相关条目与直达页面动作；原“搜索与高级用法”分区更名为“搜索语法”。
- 帮助内轻量搜索：覆盖任务指南、遇到问题、设置说明与更新亮点，按“标题 > 关键词 > 摘要”命中排序，点击结果直达文章；无结果时给出建议。
- 页面“?”入口：文件 / 项目 / 学业 / 设置四个主页面页头新增帮助按钮，直达对应文章或分区；`PageHeader` 以可选 `actions` 插槽实现，不传时渲染结构不变。
- 更新亮点：帮助中心“新手入门”顶部展示当前版本（0.8.4）用户视角亮点，随版本注册表维护，无当前版本条目时隐藏。
- 文章反馈：每篇帮助文章底部“有帮助 / 没帮助”本地记录（`rootup.help.feedback.v1`），后投覆盖先投，不上传、无后端接口。
- 空状态指引：文件无监控目录 / 搜索无结果、项目为空、学业无课程 / 无作业时新增次要帮助入口，保留原有主操作按钮。
- 文案规范：新增 `docs/COPYWRITING.md`（参考 Microsoft / Google 写作指南与中文文案排版指北），帮助文案禁用 AI 腔表达与内部实现标识符，质量门禁入测试。

## [0.8.4] - 2026-08-07

### 修复（0.8.4 前置加固）

- 归档事务原子化：`IndexStore` 新增 `archive_record` / `unarchive_record`，记录迁移与操作日志（或 undone 标记）在单事务内完成；DB 失败时磁盘文件自动回滚，撤销改为逐项即时标记，消除半成品窗口。
- 设置版本盖章：`set_settings` 写入前强制 `Settings::normalize()`，schema 版本一律以 `CURRENT_VERSION` 为准；前端兜底常量同步为 3。
- 托盘自动归档开关补发 `settings-changed` 事件，前端设置/文件页即时刷新。
- 归档快捷方式目标重映射改为大小写安全的分段前缀匹配（Windows 下 `C:/proj` 不再误命中 `C:/proj2`）。
- 重命名索引收敛：`RenamedFrom` 事件立即将旧路径标记 deleted 并广播，文件列表不再残留旧路径幽灵记录；`next_state` 状态机同步更新。
- 默认忽略规则单一来源：后端 `IgnoreMatcher::new()` 改为从 `Settings::default().ignore_rules` 构造；前端预设与兜底统一引用 `DEFAULT_IGNORE_RULES`；两侧均对共享 fixture 断言。
- 提醒语义一致性：Rust 与 TS 测试共同消费 `fixtures/reminder-cases.json`，锁定日期差、临期边界与分组语义。
- 公共时间工具：新增 `infra/time.rs::now_millis()`，替换四处重复实现。

### 重构

- `refresh_managed_state` 从组合根 `app.rs` 迁至 `infra/managed_state.rs`，命令层与托盘不再反向依赖 `app.rs`；`QuitFlag` 同步迁至 `infra/window.rs`。
- 新增 `scripts/check-rust-arch.ps1`（`npm run check:arch:rust`）并在 CI 强制执行：`core` 保持纯业务、`commands` 不回指组合根、`infra` 不依赖命令层。

### 新增（0.8.4）

- 扫描器接口化：新增 `FileEnumerator` 与 `ScanDiffStore` 契约，walkdir 默认实现；差集从内存快照改为 SQLite 临时表 / keyset，内存占用 O(批次)。
- SQLite 调优与存储治理：应用 PRAGMA 组（WAL/synchronous/mmap/cache/busy_timeout 等）、墓碑记录 30 天物理清理（schema v3 `deleted_at`）、损坏备份保留最近 3 份、快捷图标对账、设置保存 500ms 防抖合并写。
- 启动与后台优化：setup 各阶段耗时埋点、监听/扫描/自动归档/托盘延迟到前端就绪后启动（含 10s 回退）、scanner 与自动归档改为 Condvar 事件驱动、日志 200ms 周期落盘。
- 文件列表排序与翻页：名称/类型/大小/修改时间/标签五维升降序（确定性规则），页码可感知，加载更多保持滚动位置。
- 统一输入边界：目录输入统一校验（清洗/环境变量展开/非法字符/保留名/盘根拒绝）、深链参数白名单、前端即时校验镜像与后端强校验双保险。
- jwalk 评估：0.8.4 不引入依赖，结论与 0.8.5 触发条件见 `benchmarks/jwalk-evaluation.md`。
### 报告与审查

- 0.8.4 边界与职责审查、UI 审查清单与版本汇报见 [docs/reports/](docs/reports/)（`0.8.4-boundary-review.md` / `0.8.4-ui-review-checklist.md` / `0.8.4-release-report.md`）。
### 修复（UI 审查反馈）

- 搜索框存在筛选标签时，清空与语法帮助按钮不再绝对定位漂移：改为输入行内 flex 子项，与输入框天然同高居中。
- 修复多标签/组合查询的 SQL 占位符冲突：多个 `label:`（含标签 + 文本/类型/状态组合）此前会报参数数量不匹配导致文件列表加载失败；现为同维度多值 OR、跨维度 AND，并补回归测试。
- 修复筛选习惯持久化：前端 `lastUsed` 与后端 `last_used` 字段名不一致导致每次保存失败；后端改为 camelCase 序列化并兼容读取旧 snake_case 数据。
- 文件列表卡片最小高度改为仅在加载/空结果时生效：条目少时卡片随内容收缩，底部计数栏紧贴列表，不再出现大片空白。

## [0.8.3] - 2026-08-06

### 新增

- 自绘下拉 Select：共享 `Select` 重写为自定义下拉（portal 弹层、输入过滤、键盘导航、视口钳制与底部翻转、图标/色点/描述/选中勾），替换设置页、课程/作业表单、课程表、项目打开、分类映射共 9 处原生下拉；`TimeSelect` 保持不变。
- 项目页体验：共享 `DirectoryAdder`（原生“浏览…”、文件夹拖拽、常用目录一键添加、粘贴清洗与实时校验，设置页与项目页同源）；项目卡片显示来源徽章（手动/自动）与识别依据（命中特征文件名）。
- 项目识别修复：特征矩阵扩充 C++（CMake）、PHP、Ruby、Dart、Flutter、Kotlin、Swift、Android 八类；向上查找项目根跳过噪音目录（node_modules/target/dist/.git/__pycache__ 等）；`ProjectInfo` 新增 `source` 与 `detectedBy` 字段；新类型 IDE 候选与项目徽章同步补齐。
- 托盘图标：多尺寸 ICO（16/20/24/32/48/64）替换单张 PNG，临期/逾期作业时托盘红点角标随 `refresh_tray` 切换（数量仍由 tooltip 承担）。
- 设置与帮助中心观感统一：设置弹窗品牌标题 + 等高竖线、帮助中心分区标题统一层级、分区装饰分隔线（渐隐发丝线）、搜索语法表正文层级修正。
- 发布门禁：新增 `scripts/pre-release-check.ps1`，将 AI 真实全链路验收（`--open-project` / `--open-homework` 深链意图领取、截图与日志断言）纳入版本提交前的强制检查；修复首次启动深链事件早于网页监听器就绪导致不跳转的问题。
- 打开链路加固：PATH 查找只接受 Windows 可执行扩展名（跳过 VS Code 等无扩展名 shell shim），`.cmd/.bat` 经 `cmd /C` 包装启动，修复 `code.cmd` 无法打开 IDE 的问题；项目唤醒（`--open-project`）不再调起前台窗口（运行中不抢焦点、首次启动隐藏驻留托盘），作业唤醒保持聚焦。
- 性能基准体系（自研唯一标准，零新依赖）：引擎 harness（`cargo bench --features bench`：扫描 10k/100k、10 万条查询、标签重分类、归档撤销）+ 系统基准（`scripts/benchmark.ps1`：冷启动/扫描/内存/产物体积，5 轮中位数）+ `scripts/render-benchmarks.ps1`（README 对比表与 SVG 趋势图）+ `pre-release-check.ps1 -Bench` 门禁冒烟（15% 警示规则）；0.8.3 初始基线已入库。
- 性能基准升级 v2（仅本地运行）：真实语料 `corpus.json`（大小分布/宽深树/噪声/Unicode 长路径）、时间维度（冷/暖启动、暖重扫、空闲 CPU、内存序列）与空间维度（索引库体积、churn/VACUUM、进程 IO、产物体积）、schema v2（p50/p90/p99/CV + host 指纹）；`scripts/bench-all.ps1` 一键执行、`scripts/bench-cleanup.ps1` 清理本机残留；0.8.3 v2 基线重跑入库，旧 v1 结果归档并标注不可比。
- 打开链路无黑窗：`.cmd/.bat` 工具（如 VS Code `code.cmd`）经 `cmd /C` 包装启动时附加 `CREATE_NO_WINDOW`，不再弹出命令行窗口，目标 IDE 窗口不受影响。
- 文件列表 UI 定稿：移除路径行、日期列与列间分割线（悬浮仍可看完整路径）；文件名列固定 224px、其余列定宽且文字左对齐，类型/大小/状态列内容超出即截断并悬浮显示全文；类型与大小改用等宽字体、大小单位适度突出，标题与后续列间距加大；标签移到文件名下方并强制单行，课程类标签优先、首个标签完整展示、其余折叠进“+N”悬浮查看；操作按钮放大为 md 并加大间距、悬停时在弹性留白区居中浮现，“智能打开”（特殊工具/系统默认）与“用 IDE 打开”（强制项目 IDE，仅代码/文本类文件显示）语义分离，新增“复制路径”操作，状态列仍贴右对齐可见；标签键去重并覆盖行内、筛选与搜索补全，同名课程（不同学期/时间的同一门课）统一为一个标签（分类器按学期顺序取稳定 key，前端按显示名二次去重）。
- 性能基准 v2 标定补全：churn 测量先 `wal_checkpoint(TRUNCATE)` 再取样并对 VACUUM 加“不得变大”硬断言；引擎结果补齐 `date` 与 host 指纹；系统基准补入 interactive 与 IO 读写指标，DryRun 自校验必含键；`bench-all` 在系统基准前重建 release（`cargo bench` 会覆盖 exe 导致前端不加载）；0.8.3 官方基线按“引擎 Full + 系统 10k”重跑为 50 项指标。
- 验收清理补强：代理验收在启动前快照现有 IDE 进程，结束时只关闭本次拉起的 IDE 与 rootup，并断言无测试拉起的 IDE 残留，避免临时夹具被锁。
- 基准可复现/可比较契约：host 指纹扩展 node / npm / RAM；`bench-all` 增加确定性语料自检（同 seed 200 文件 × 2 哈希一致）；渲染器按指纹分组，只有同主机版本才计算 delta 与 15% 警示，跨主机/缺指纹/legacy v1 标记不可比；基线 JSON、对比表与 SVG 随版本入库并在 README 展示。

## [0.8.2] - 2026-08-06

### 新增

- 监控目录体验加固：移除目录即清理索引（只标记 deleted、可重扫恢复，确认弹窗显示数量）；新增原生“浏览…”目录选择器（tauri-plugin-dialog）、文件夹拖拽（文件自动取父目录）、常用目录一键添加（下载/桌面/文档）、粘贴路径清洗与实时校验。
- 灵动视觉与微交互：动效令牌（`--duration-*` / `--ease-*`）与全局 `prefers-reduced-motion` 降级；列表入场与关键反馈动画；按钮/图标按钮/Chip 按压微动；共享 `Tooltip` 组件并接入所有 IconButton，提示与 aria 文案统一。
- 设置与说明中心体验：设置行“点行看说明、尾部编辑按钮才编辑”；新增 `SettingsInfoDialog`（功能介绍/情景举例/设置说明）与 `lib/settingsGuide.ts` 单一说明数据源；说明中心新增“设置说明”分区；设置页分组描述与留白、文件/作业/项目页轻度密度治理。

## [0.8.1] - 2026-08-06

### 新增

- 作业截止提醒：默认关闭、可设置提前量（1–14 天）；学业页顶部提示条（可关闭本次）、作业列表按“已逾期 / 临期”分组置顶，托盘 tooltip 显示临期计数并提供直达入口。
- 关闭默认行为持久化：新增 `close_action`（ask / background / quit，默认 ask，Settings v2→v3 迁移）；关闭确认弹窗支持“记住我的选择”，Rust 侧按设置直接后台运行或退出。
- 语言切换改为下拉列表；新增前端语言注册表（`lib/languages.ts`），明确“新增语言 = 前端字典 + 注册表 + 后端白名单”三处同步约定。
- 设置页按“常规 / 监控与分类 / 归档 / 学业提醒 / 高级”分组（FormSection），新增关闭行为、提醒开关与提前量、桌面“打开未完成作业”快捷方式入口。
- 托盘菜单改进：左键单击打开主窗口；菜单按学业数据与设置动态重建（不轮询），含临期作业直达、自动归档开关、主题快速切换；新增 `--open-homework` 启动参数与 `study-homework-open` 事件深链。

## [0.8.0] - 2026-08-05

### 新增

- 学业页 UI 第一版：侧栏“作业 / 课程”合并为“学业”；课程表（时间轴周视图，周起始日可切周一 / 周日，支持全周 / 单周 / 双周 / 指定周次与“仅当前周”过滤）与作业（截止排序、状态 / 课程筛选、勾选完成、归档、删除确认）两个平级视图。
- 共享分段切换组件 `SegmentedControl`；课程 / 作业表单弹窗（前端内存示例数据，未接后端持久化）。
- 中英双语时间格式：中文 24 小时制 / English 12 小时 AM·PM。
- 学业页体验重塑：课程表视觉重设计（周头日期与“今天”药丸、当前时间线、课程卡色条与柔和投影）；表单按分区重排并统一文字层级；新建课程颜色默认自动分配。
- 作业区分短备注（≤200，行内显示）与长详情（≤5000，可展开，AI 摘要预留字段）；修复“N 天后截止”数字丢失并新增“已逾期 N 天”；标记完成走确认弹窗。
- 全应用按钮顺序统一为 Windows“是左否右”（主操作在左、取消在右）；新增共享组件 `Field` / `ColorPicker` / `DialogFooter` / `DueText`；`Modal` 标题层级提升。
- 输入法保护：键盘监听在拼音组合期间不再拦截按键（`isComposing` 保护）；窗口失焦时主动释放输入焦点，减少残留输入状态对其它窗口输入法的影响（系统级输入法问题属 Windows/微软拼音已知行为，本项为保护性修复）。
- 编辑弹窗观感收敛：课程 / 作业表单改用分隔线分区并统一紧凑间距（新增共享组件 `FormSection` / `TextArea`）；课程表内部胶囊圆角收敛为小圆角，容器与课程卡各保留一个圆角。
- 学业页 UI 重构：修复分区分割线贴输入框（FormSection 改为自身 `border-t` + 双向 16px 间距）；分区标题升级为强色 + 品牌竖条；课程表单按语义重排字段（星期全宽、开始/结束组成“至”时间对、指定周次按需出现）；`SegmentedControl` 新增 tabs 下划线变体并放宽分段内边距；作业筛选加状态/课程小标题；课程表方形化、作业行 12px 圆角。

- 学业页课程表“堆叠磁贴”重构：主切换升级为等宽双栏 + 图标 + 待办数；时间刻度移到表格外、删除内部左列、弱化网格线；课程卡轻磁贴化（圆角收敛至 4px/2px 级、内容按高度降级、短课时最小高度）；错周同槽合并为堆叠行、同周重叠最多两栏并折叠 “+N”；点击课程进入只读详情（完整信息 + 作业列表），编辑/删除/查看作业显式操作；补齐长名称、短课时、错周聚合、多课溢出等测试。
- 学业页表单与深浅色体验加固：自定义 `TimeSelect`（5 分钟粒度、深色适配）替换原生时间控件；课程表单重排（名称+老师同行右对齐、地点独占一行）与智能时间（默认时长自动调整、课程时长快捷项、同周冲突实时提示、周次范围容错、字段级错误与底部错误）；作业表单日期+时间拆分、逾期提示、按课程时间建议截止；课程详情作业行可点击并自动展开详情、状态点按状态/逾期变色；顶部切换限宽与深色徽标修复；全局 color-scheme/placeholder 深色适配；偏好记忆与自动聚焦等静默智能。
- 学业页微调：时间刻度移回表体左侧并对齐网格线（不再与周一表头并排）；课程表单名称加长（5/7）与老师（2/7）同行、地点随弹窗加宽；指定周次去除重复说明；顶部切换进一步缩短；同周重叠新增硬性合法性校验（保存时拦截并明确报错），移除示例中的重叠课程。
- 学业页布局修正：时间刻度移回表格外侧并与表体对齐（按表头实测高度偏移，不再与周一表头错位或进入表格）；示例新增与“程序设计”同槽错周的“大学物理”，堆叠卡片带间距展示；同周冲突保持保存拦截。
- 错周同槽改为“堆叠点击展开”：折叠时只显示首门课 + 层叠边缘 + “共 N 门”角标，体现同时段与层次；点击展开为该时段全部课程的可读卡片行，并可收起；点击行内课程仍进详情。
- 堆叠卡体验细化：课程按上课顺序堆叠；自动配色避开同课时段已有颜色以作区分；角标简化为纯数字；展开改为浮层平铺在课表上方（逐张 fan-in 动效），支持 Esc/点外部/关闭按钮收起。
- 堆叠卡铺开浮层：点击后原卡隐藏，课表上方平铺完整信息卡（课程名/时间/周次/老师/地点/作业数），deal-in 逐张摊开动效，Esc/遮罩/关闭按钮收起；新增长标题示例课程与“图标 + 数字”角标。
- 铺开浮层去窗口化：卡片组居中悬浮在页面上，无面板/边框/遮罩感，原堆叠卡隐藏，关闭后恢复。
- 铺开改为独立元素卡阵：每张卡固定定位在点击位置附近摊开，无容器/滚动框；卡片加大并完整展示名称/老师/地点；最多平铺 4 张，超出以 +N 收口；堆叠卡提升层级避免被相邻课程盖住。
- 堆叠层叠边缘改为“可越界但被其它课程卡覆盖”：堆叠卡不设更高 z-index，其它课程卡渲染在其上层。
- 课程表学期生命周期：学期选择（双语）、当前周计数与手动预览周、回到本周、学期日期范围提示；工具栏分层展示，学期偏好持久化。
- 学期管理（课表创建与保存）：新建/编辑/复制/删除学期，每学期独立课程与作业，切换学期即切换课表；数据持久化到 localStorage `rootup.study.data.v1`，首次启动自动迁移示例。
- 学业页微调：添加课程按钮移至课表上方工具行右侧（紧贴课程表）；学期管理/持久化测试补足（存储校验、弹窗交互、按学期隔离与持久化、损坏回退等）。
- 学业数据后端化：`study.json` 统一管理（原子写/损坏备份），旧 localStorage 数据首次启动迁移并清除；种子扩为场景与边界数据（短课时/周末课/三张堆叠/长标题/各作业状态），迁移时并入“演示：边界场景”学期。
- 课程分类链路：`StudyClassifier` 按完整课程名（长名优先）打 `course-<id>` 标签；学业保存后定向重分类存量文件（仅更新 labels 列），文件页/筛选/补全展示课程名与颜色。
- 堆叠与铺开层次优化：折叠堆叠最多两层（主卡 + 一层边缘），顶卡为牌堆最上层；铺开卡阵视口居中，前景卡加强投影/悬停抬升并加淡遮罩区分前后景。
- 铺开卡阵细节：移除右上角关闭按钮（保留 Esc/点外部关闭），多行卡阵纵向间距收窄至 8px。
- 作业视图默认只显示活跃（待办 + 已完成），已归档自动隐藏，已归档/全部筛选置于最后。
- 统一本地存储层：labels/schemes/habits/study 四个领域 JSON 收敛到 `infra/local_file.rs`（读取 / 原子写 / 损坏备份单一实现，行为等价）；settings 仍由 tauri-plugin-store 管理。
- 前端目录领域化：`features/study` 与 `features/settings` 的页面级 UI 与测试收进各自 `components/` 子目录，`pages/` 只保留页面壳。
- 发布前审查清理：修复标签筛选前缀误匹配（`course-1` 不再命中 `course-10`）、监听/自动归档线程启动竞态、文件稳定采样间隔生效、归档批次号进程内单调分配、项目归档/撤销中途失败完整回滚（目录/settings/快捷方式三态还原）、定向重分类批量事务写入；学业页仅在用户变更后整份保存（不再每次进入触发全量重分类）；设置与筛选习惯的副作用保存重构；移除未使用命令 `list_files`/`scan_now`。
- 发布准备：版本切换至 0.8.0；作业截止提醒顺延至下一迭代，本轮不做新功能。
- 稳定性修复：课程/作业排序固定中文规则（名称 `zh-CN` 排序、id 按码点比较），消除 CI 与本地运行时 locale 差异导致的堆叠卡测试失败。

### 后续规划

- 作业截止提醒（临期提醒）顺延至下一迭代，默认关闭并提供提前量设置。
- AI 能力（分类与项目识别增强）在学业数据落地后接入，接口已预留。

## [0.7.0] - 2026-08-04

### 新增

- 标签管理设置弹窗：自定义标签注册表（显示名 / 图标 / 预设色板），内置大类只读；筛选行、搜索标签与文件徽标统一取注册表显示。
- 受控归档与撤销：单文件归档（单击执行 + 撤销）、批量归档（批量模式多选 / 归档当前筛选结果，严格确认）、项目文件夹作为单元整体归档并联动桌面快捷方式、设置页“归档设置”（归档根 / 自动归档开关 / 最近归档可撤销）。
- 自动归档开关：新出现且分类明确（非 other）的稳定文件自动归档，默认关闭，文件页常驻轻提示。
- 反馈入口：GitHub Issue / PR 模板。

### 内部

- 版本号规则与发布纪律落地：`npm run check:version` 五处一致性校验，CI 与 release 流水线强制执行；安装包验证版本自动取自 `tauri.conf.json`。

## [0.6.0] - 2026-08-04

首个公开发布（NSIS 安装包 + 自动验证）。

### 新增

- NSIS 安装包：per-user 安装（免管理员）、安装语言可选简体中文 / English、开始菜单目录 RootUp。
- 品牌图标全套：应用窗口、安装器与平台资源统一为 RootUp sprout 标识。
- 安装包自动验证脚本 `scripts/verify-installer.ps1`（静默安装 → 冒烟 → 卸载全链路）。
- GitHub Release 流水线：打 `v*` tag 自动构建、验证并上传安装包（附 CHANGELOG 摘要）。

### 说明

- 安装包未做数字签名，SmartScreen 首次运行提示“未知发布者”属预期，功能不受影响。

## [0.5.0] - 2026-08-04

UI 复用抽取与新手引导。

### 新增

- 共享组件：Input / Select / InlineNotice / EmptyState / PageHeader / SyntaxTable，替换手写重复样式。
- 侧栏全局“帮助与新手入门”入口与分组帮助中心（新手入门 / 搜索与高级用法）。
- 首次启动欢迎弹窗（一次性标记，可重看）。
- IDE 选择与官方下载指导；检测到代码项目但无 IDE 时按需引导。
- “当前检测到”工具提示；`list_detected_tools` 与 `open_url`（https 官方域名白名单）。

## [0.4.0] - 2026-08-04

项目识别与智能打开。

### 新增

- 项目页：监控目录自动发现 + 手动添加，项目类型徽章（Rust/Node/Python/Java/C#/Go/Unity/通用）。
- 特征文件项目识别与向上找项目根；应用查找成熟机制（PATH / App Paths 注册表 / 常见路径 / 自定义命令）。
- IDE 与常用工具智能打开（VS Code、JetBrains、MATLAB、Typora、Obsidian 等），找不到回退系统默认。
- 文件行操作：打开 / 在资源管理器中显示 / 用 IDE 打开。
- 桌面快捷方式指向 RootUp `--open-project` 智能唤起（单实例传参）；项目图标预置。

## [0.3.0] - 2026-08-04

全面审查加固。

### 变更

- 修复文件列表实时合并不截断已加载分页；监控目录返回规范化路径。
- 组件测试设施（@testing-library/react + jsdom），UI 边界自动化。
- `ui:` 行为日志与 `settings: 加载` 日志；冒烟脚本断言扩展。
- CI：完整 tauri 构建、失败日志 artifact、smoke job、架构依赖校验。

## [0.2.0] - 2026-08-04

文件监听与索引、文件门户与配置体系。

### 新增

- 文件监听与索引：全量扫描、实时索引、快照差集与误删防护、删除风暴保护。
- 分类标签与搜索语法（`type:` / `label:` / `state:` / `size:` / `before:` / `after:`）。
- 搜索自动补全与频率排序筛选；习惯数据收归应用管理。
- 可配置规则：忽略规则、分类映射、规则模板与自定义方案。
- 日志系统、浮窗与皮肤基础、设置弹窗体验统一。

## [0.1.0] - 2026-08-04

框架与 UI 基础。

### 新增

- Tauri v2 + React + TypeScript + Tailwind 架构；单向依赖分层。
- 系统托盘、关闭即销毁 / 托盘重建、单实例唤起。
- 主题三态（浅色 / 深色 / 跟随系统）、中英双语切换、设置持久化。
- 左导航五页面骨架（文件 / 项目 / 作业 / 课程 / 小工具 / 设置）。
