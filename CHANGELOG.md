# Changelog

本项目的版本变更记录。首个公开发布为 v0.6.0；此前版本为开发期里程碑，按迭代整理。

## [Unreleased]

### 新增（v0.8.0-dev）

- 学业页 UI 第一版：侧栏“作业 / 课程”合并为“学业”；课程表（时间轴周视图，周起始日可切周一 / 周日，支持全周 / 单周 / 双周 / 指定周次与“仅当前周”过滤）与作业（截止排序、状态 / 课程筛选、勾选完成、归档、删除确认）两个平级视图。
- 共享分段切换组件 `SegmentedControl`；课程 / 作业表单弹窗（前端内存示例数据，未接后端持久化）。
- 中英双语时间格式：中文 24 小时制 / English 12 小时 AM·PM。
- 学业页体验重塑：课程表视觉重设计（周头日期与“今天”药丸、当前时间线、课程卡色条与柔和投影）；表单按分区重排并统一文字层级；新建课程颜色默认自动分配。
- 作业区分短备注（≤200，行内显示）与长详情（≤5000，可展开，AI 摘要预留字段）；修复“N 天后截止”数字丢失并新增“已逾期 N 天”；标记完成走确认弹窗。
- 全应用按钮顺序统一为 Windows“是左否右”（主操作在左、取消在右）；新增共享组件 `Field` / `ColorPicker` / `DialogFooter` / `DueText`；`Modal` 标题层级提升。
- 输入法保护：键盘监听在拼音组合期间不再拦截按键（`isComposing` 保护）；窗口失焦时主动释放输入焦点，减少残留输入状态对其它窗口输入法的影响（系统级输入法问题属 Windows/微软拼音已知行为，本项为保护性修复）。
- 编辑弹窗观感收敛：课程 / 作业表单改用分隔线分区并统一紧凑间距（新增共享组件 `FormSection` / `TextArea`）；课程表内部胶囊圆角收敛为小圆角，容器与课程卡各保留一个圆角。
- 学业页 UI 重构：修复分区分割线贴输入框（FormSection 改为自身 `border-t` + 双向 16px 间距）；分区标题升级为强色 + 品牌竖条；课程表单按语义重排字段（星期全宽、开始/结束组成“至”时间对、指定周次按需出现）；`SegmentedControl` 新增 tabs 下划线变体并放宽分段内边距；作业筛选加状态/课程小标题；课程表方形化、课程卡 8px 圆角、作业行 12px 圆角。

### 规划中

- 学业数据持久化（按 `lib/study.ts` 类型落地后端）、截止提醒、课程关键词纳入分类规则。
- AI 能力（分类与项目识别增强）在学业数据落地后接入，接口已预留。

## [0.7.0] - 2026-08-04

### 新增

- 标签管理设置弹窗：自定义标签注册表（显示名 / 图标 / 预设色板），内置大类只读；筛选行、搜索标签与文件徽标统一取注册表显示。
- 受控归档与撤销：单文件归档（单击执行 + 撤销）、批量归档（批量模式多选 / 归档当前筛选结果，严格确认）、项目文件夹作为单元整体归档并联动桌面快捷方式、设置页“归档设置”（归档根 / 自动归档开关 / 最近归档可撤销）。
- 自动归档开关：新出现且分类明确（非 other）的稳定文件自动归档，默认关闭，文件页常驻轻提示。
- 反馈入口：GitHub Issue / PR 模板。

### 内部

- 版本号规则与发布纪律落地：`npm run check:version` 五处一致性校验，CI 与 release 流水线强制执行；安装包验证版本自动取自 `tauri.conf.json`。

## [0.6.0] - 2026-08-04

首个公开发布（NSIS 安装包 + 自动验证）。

### 新增

- NSIS 安装包：per-user 安装（免管理员）、安装语言可选简体中文 / English、开始菜单目录 RootUp。
- 品牌图标全套：应用窗口、安装器与平台资源统一为 RootUp sprout 标识。
- 安装包自动验证脚本 `scripts/verify-installer.ps1`（静默安装 → 冒烟 → 卸载全链路）。
- GitHub Release 流水线：打 `v*` tag 自动构建、验证并上传安装包（附 CHANGELOG 摘要）。

### 说明

- 安装包未做数字签名，SmartScreen 首次运行提示“未知发布者”属预期，功能不受影响。

## [0.5.0] - 2026-08-04

UI 复用抽取与新手引导。

### 新增

- 共享组件：Input / Select / InlineNotice / EmptyState / PageHeader / SyntaxTable，替换手写重复样式。
- 侧栏全局“帮助与新手入门”入口与分组帮助中心（新手入门 / 搜索与高级用法）。
- 首次启动欢迎弹窗（一次性标记，可重看）。
- IDE 选择与官方下载指导；检测到代码项目但无 IDE 时按需引导。
- “当前检测到”工具提示；`list_detected_tools` 与 `open_url`（https 官方域名白名单）。

## [0.4.0] - 2026-08-04

项目识别与智能打开。

### 新增

- 项目页：监控目录自动发现 + 手动添加，项目类型徽章（Rust/Node/Python/Java/C#/Go/Unity/通用）。
- 特征文件项目识别与向上找项目根；应用查找成熟机制（PATH / App Paths 注册表 / 常见路径 / 自定义命令）。
- IDE 与常用工具智能打开（VS Code、JetBrains、MATLAB、Typora、Obsidian 等），找不到回退系统默认。
- 文件行操作：打开 / 在资源管理器中显示 / 用 IDE 打开。
- 桌面快捷方式指向 RootUp `--open-project` 智能唤起（单实例传参）；项目图标预置。

## [0.3.0] - 2026-08-04

全面审查加固。

### 变更

- 修复文件列表实时合并不截断已加载分页；监控目录返回规范化路径。
- 组件测试设施（@testing-library/react + jsdom），UI 边界自动化。
- `ui:` 行为日志与 `settings: 加载` 日志；冒烟脚本断言扩展。
- CI：完整 tauri 构建、失败日志 artifact、smoke job、架构依赖校验。

## [0.2.0] - 2026-08-04

文件监听与索引、文件门户与配置体系。

### 新增

- 文件监听与索引：全量扫描、实时索引、快照差集与误删防护、删除风暴保护。
- 分类标签与搜索语法（`type:` / `label:` / `state:` / `size:` / `before:` / `after:`）。
- 搜索自动补全与频率排序筛选；习惯数据收归应用管理。
- 可配置规则：忽略规则、分类映射、规则模板与自定义方案。
- 日志系统、浮窗与皮肤基础、设置弹窗体验统一。

## [0.1.0] - 2026-08-04

框架与 UI 基础。

### 新增

- Tauri v2 + React + TypeScript + Tailwind 架构；单向依赖分层。
- 系统托盘、关闭即销毁 / 托盘重建、单实例唤起。
- 主题三态（浅色 / 深色 / 跟随系统）、中英双语切换、设置持久化。
- 左导航五页面骨架（文件 / 项目 / 作业 / 课程 / 小工具 / 设置）。
