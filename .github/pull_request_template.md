## 变更说明

（这次改了什么、为什么改）

## 测试与验证

- [ ] 前端测试（`npm test`）通过
- [ ] 前端构建（`npm run build`）通过
- [ ] Rust 测试 / clippy / fmt 通过
- [ ] 版本一致性（`npm run check:version`）通过
- [ ] 架构校验（`npm run check:arch`）通过
- [ ] 冒烟（`scripts/smoke.ps1`）通过（涉及运行时改动时）

## 关联 Issue

Closes #
